"""
Backup Manager Module

This module provides functionalities to manage AWS Backup plans, including:
    - Creating new backup plans interactively or via JSON configurations.
    - Deploying existing backup plans with or without a dry-run mode.
    - Updating and deleting backup plans.

Modules:
    - create_plan: Handles the creation of backup plans.
    - deploy_plan: Handles the deployment of backup plans.
    - update_plan: Handles updates to existing backup plans.
    - delete_plan: Handles deletion of backup plans.
"""
